import {StyleSheet} from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center"
  },

  datePickerContainer: {
    width: 250,
    margin: 20,
  },

  datePickerLabel: {
    fontSize: 18,
    fontWeight: "bold",
  },
});